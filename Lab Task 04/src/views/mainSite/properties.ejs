<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Main Site - Properties</title>
</head>
<body>
  <h1>Main Site - Properties</h1>

  <% if (properties.length === 0) { %>
    <p>No properties available on the main site.</p>
  <% } else { %>
    <ul>
      <% properties.forEach(property => { %>
        <li><a href="/properties/<%= property._id %>"><%= property.name %></a></li>
      <% }); %>
    </ul>
  <% } %>

  <a href="/admin/dashboard">Go to Admin Dashboard</a>
</body>
</html>
